package com.niltonrc.simplef.contracts;

import com.niltonrc.simplef.messages.*;

public interface IEntryService
{
    ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
    // Static fields
    ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
    // Static Methods
    ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
    // Default Methods
    ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

    ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
    // Contracts
    ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

    /**
     * Get statistics
     */
    StatisticsResponse getStatistics( StatisticsRequest statisticsRequest );

    /**
     * "swap" fake address to a real address and add hits to path
     */
    SwapResponse swap( SwapRequest swapRequest );

    /**
     * A simple way to add/create a new forwarding
     */
    CreateForwardingResponse createForwarding( CreateForwardingRequest createForwardingRequest );
}
